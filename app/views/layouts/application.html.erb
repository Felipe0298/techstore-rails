<!DOCTYPE html>
<html>
  <head>
    <title>Techstore</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div class="flex justify-center items-center">
      <%= link_to image_tag("techstore.png", alt: "Logo Techstore"), products_path, class: "w-100 h-100" %>
    </div>

    <div class="my-5 flex flex-row flex-wrap justify-evenly">
      <%= link_to "Inicio", products_path, class: "text-4xl hover:text-5xl transition-all duration-400" %>

      <% if @categories.present? %>
        <% @categories.each do |category| %>
          <%= link_to "#{category}", products_path(category: category), class: "text-4xl hover:text-5xl transition-all duration-400" %>
        <% end %>
      <% end %>

      <%= link_to cart_path do %>
        <span class="cart-icon text-4xl hover:text-5xl transition-all duration-400">&#128722;</span>
        <% if @cart_products_count > 0 %>
          <span class="cart-count text-4xl hover:text-5xl transition-all duration-400"><%= @cart_products_count %></span>
        <% end %>
      <% end %>
    </div>

    <div>
      <% if current_page?(products_path) %>
        <% if @select_category.present? %>
          <h1 class="text-3xl flex justify-center items-center mt-10">Todos nuestros productos <%= @select_category %></h1>
        <% else %>
          <h1 class="text-3xl flex justify-center items-center mt-10">Todos nuestros productos</h1>
        <% end %>
      <% end %>
    </div>
    
  <% flash.each do |type, message| %>
    <div class="fixed top-0 right-0 m-4">
      <div class="bg-green-500 text-white px-4 py-2 rounded-lg shadow-md">
        <div class="flex items-center">
          <% if type == 'notice' %>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
          <% elsif type == 'alert' %>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
          <% end %>
          <p><%= message %></p>
        </div>
      </div>
    </div>
  <% end %>

  
    <%= yield %>
  </body>
</html>
